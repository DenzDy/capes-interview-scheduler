[project]
name = "capes-interview-scheduler"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "gspread>=6.2.1",
    "matplotlib>=3.10.7",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
